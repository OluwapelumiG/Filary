/** Local Express re-exports the Vercel-bundled generate module (single source of truth). */
export {
  generateValues,
  type FormFieldDescriptor,
  type GenerateRequest,
  type GenerateResponse,
  type GenerateOptions,
  type FieldValue,
} from '../../api/lib/generate.js';
